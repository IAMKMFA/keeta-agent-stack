{
  "name": "keeta-agent-sdk",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "pnpm": {
    "overrides": {
      "protobufjs": "^7.5.5"
    }
  },
  "scripts": {
    "dev": "turbo run dev --filter=@keeta-agent-sdk/api --filter=@keeta-agent-sdk/worker --filter=@keeta-agent-sdk/dashboard",
    "dev:all": "turbo run dev --filter=@keeta-agent-sdk/api --filter=@keeta-agent-sdk/worker --filter=@keeta-agent-sdk/dashboard --filter=@keeta-agent-sdk/mcp",
    "dev:mcp": "pnpm --filter @keeta-agent-sdk/mcp dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:integration": "RUN_INTEGRATION_TESTS=true pnpm --filter @keeta-agent-sdk/integration-tests test",
    "lint": "turbo run lint",
    "lint:security": "turbo run lint:security",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules",
    "db:generate": "pnpm --filter @keeta-agent-sdk/storage exec drizzle-kit generate",
    "db:migrate": "pnpm --filter @keeta-agent-sdk/storage exec tsx src/migrate.ts",
    "db:migrate:down": "pnpm --filter @keeta-agent-sdk/storage exec tsx src/migrate-down.ts",
    "db:seed": "pnpm --filter @keeta-agent-sdk/storage exec tsx src/seed.ts",
    "demo": "bash scripts/demo.sh",
    "docs:generate": "typedoc",
    "docs:serve": "pnpm --filter @keeta-agent-sdk/api dev",
    "docs:publish": "echo 'Configure your hosting target (e.g. gh-pages, S3) before running. See docs/sdk-reference.md.'"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.2",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typedoc": "^0.28.19",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2"
  }
}
