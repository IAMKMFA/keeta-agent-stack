{
  "name": "@keeta-agent-stack/integration-tests",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && printf 'integration-tests\\n' > dist/.keep",
    "test": "vitest run --config vitest.config.ts",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@keeta-agent-stack/adapter-registry": "workspace:*",
    "@keeta-agent-stack/api": "workspace:*",
    "@keeta-agent-stack/keeta": "workspace:*",
    "@keeta-agent-stack/routing": "workspace:*",
    "@keeta-agent-stack/storage": "workspace:*",
    "@keeta-agent-stack/types": "workspace:*",
    "@keeta-agent-stack/worker": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "ioredis": "^5.4.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@keeta-agent-stack/adapter-mock-dex": "workspace:*",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "jose": "^5.9.6",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  }
}
