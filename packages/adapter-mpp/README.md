# `@keeta-agent-stack/adapter-mpp`

Simulated Machine Payments Protocol adapter for Keeta Agent Stack.

This package implements the existing `VenueAdapter` contract from `@keeta-agent-stack/adapter-base`.
In this alpha it models MPP quote/simulate behavior and returns a structured
`MPP_LIVE_NOT_CONFIGURED` failure for live execution.
