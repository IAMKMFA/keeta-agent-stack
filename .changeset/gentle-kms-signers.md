---
'@keeta-agent-stack/keeta': patch
'@keeta-agent-stack/config': patch
---

Add a Keeta signer provider abstraction with backwards-compatible seed signing and optional GCP
KMS-backed signing for worker live execution.
